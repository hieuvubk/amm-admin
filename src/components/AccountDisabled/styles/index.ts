import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  dialog: {
    '& .MuiDialog-paper': {
      width: 578,
      padding: 0,
      margin: 0,
    },
  },
}));

export default styles;
